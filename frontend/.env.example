NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=http://localhost:4000
# Get this from Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID (type: Web)
# Optional: leave blank to disable Google sign-in (email/password still works)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
